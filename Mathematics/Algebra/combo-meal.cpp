#include <iostream>

using namespace std;

int main() {
    int tc;
    cin >> tc;
    while(tc--) {
        int b, s, c;
        cin >> b >> s >> c;
        cout << b+s-c << endl;
    }
    return 0;
}
