    #include <iostream>
    using namespace std;
     
    int main()
    {
        int n,k;
        cin>>n>>k;
        int a[n];
        for(int i=0;i<n;++i)
            cin>>a[i];
        
        int c=0;
        while(c<(n-k+1))
        {
            int max= a[c];
            for(int i=c; i<c+k; ++i)
            {
                if(a[i]>max)
                    max=a[i];
            }   
            cout<<max<<" ";
            ++c;
        }
        
        return 0;
    }
