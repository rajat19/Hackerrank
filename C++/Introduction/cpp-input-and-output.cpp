#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;
//main function
int main()
{
    int input_1, input_2, input_3;

    //The "extraction operator" >> is the one we usually use for input
    cin>>input_1>>input_2>>input_3;

    //The "insertion operator" << is the one we usually use for output
    cout<<(input_1 + input_2 + input_3);
    return 0;
}
